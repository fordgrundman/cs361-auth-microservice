# Flask
PORT=5000

# JWT signing secret (keep private)
JWT_SECRET=change-me-super-secret

# App registry for trusted server-to-server calls (introspect)
# Format: comma-separated pairs appId:clientSecret
# Example for 5 apps:
APP_SECRETS=workouts-app:abc123,notes-app:def456,finance-app:ghi789,recipes-app:jkl012,study-app:mno345

# Database file path (SQLite)
# Local default is auth.db, Koyeb Volume persistence uses /data/auth.db
# Uncomment the DB_PATH line below according to your deployment environment:
DB_PATH=/data/auth.db # For Koyeb Volume persistence
# DB_PATH=auth.db # For local development (not persisted across restarts)
